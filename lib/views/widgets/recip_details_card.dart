import 'package:flutter/material.dart';

class RecipeDetailsCard extends StatelessWidget {
  const RecipeDetailsCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
